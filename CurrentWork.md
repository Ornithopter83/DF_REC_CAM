# DFBlackbox 현재 작업 상태

업데이트: 2026-07-15

## 프로젝트 개요

- 저장소: `C:\Projects\VS\DFBlackbox`
- 애플리케이션: .NET 8 Windows Forms 기반 블랙박스/감시 녹화 및 재생 도구
- 주요 기술:
  - WinForms
  - Krypton Toolkit
  - OpenCvSharp
  - FFmpeg 우선 녹화 저장, OpenCV 녹화 fallback
- 주요 설정/런타임 파일:
  - 설정: `settings.json`
  - 기준 이미지: `baseline_reference.png`
  - 녹화 폴더: `REC`
  - 로그 폴더: `Logs`

## 최근 주요 변경

### Krypton Toolkit UI 개선

- `Krypton.Toolkit`을 도입하고 메인 폼과 설정 폼을 `KryptonForm` 기반으로 변경했다.
- 전역 팔레트는 `Office2013White`를 사용하고, 공통 외관은 `DFBlackbox/Utils/UiTheme.cs`에서 관리한다.
- 메인 조작 패널의 버튼, 입력 컨트롤, 라디오 버튼, 체크박스를 Krypton 컨트롤로 변경했다.
- 설정창의 입력 컨트롤은 Krypton 컨트롤로 변경하고, 버튼은 상태별 색상 적용이 확실한 플랫 WinForms 버튼을 사용한다.
- 기본 버튼은 인디고 계열, 마우스오버는 밝은 청색, 눌림은 어두운 청색, 비활성은 회색 계열로 구분한다.
- 버튼 글씨는 굵게 표시하고, 설정창의 주요 라벨도 굵게 표시한다.
- 설정창 폭과 입력 행 너비를 넓혀 오른쪽 필드가 잘리지 않도록 조정했다.
- 비활성 `새로고침` 버튼의 글씨가 묻히지 않도록 회색 배경과 비활성 글자색을 적용했다.
- 입력 컨트롤의 활성 외곽선을 중립색으로 낮춰 내부 테두리와 겹쳐 보이는 현상을 완화했다.
- 재생 컨트롤은 원형 버튼형 디자인과 인디고 색상을 적용했으며 기존 이벤트 처리 구조는 유지한다.
- Krypton 체크박스/라디오 버튼에는 투명 배경 대신 불투명 배경을 사용해 실행 오류를 방지한다.
- UI 변경은 커밋 `d59ddc2`에 반영되어 `main`과 `origin/main`이 일치한다.

### 현지화

- 앱 언어 설정 `Language`를 `settings.json`에 저장한다.
- 기본 언어는 `KOR`이며, 기존 설정 파일에 언어 값이 없어도 정상 동작한다.
- 메인 메뉴에 한국어/영어 선택 메뉴를 추가했다.
- 메인 화면, 설정창, 최근 이벤트창, 카메라 속성창의 주요 문구를 KOR/ENG로 표시한다.
- 상태바, 메시지박스, 트레이 메뉴, 파일 열기 대화상자의 주요 문구도 현지화했다.
- 공용 현지화 유틸은 `DFBlackbox/Utils/Localization.cs`에 있다.

### 동영상 파일 드래그드롭 재생

- 영상 표시 영역에 동영상 파일을 드래그하면 지원 여부를 시각적으로 표시한다.
- 지원 확장자:
  - `.mp4`
  - `.avi`
  - `.mov`
  - `.mkv`
  - `.wmv`
- 지원 파일 드롭 시 기존 재생 모드로 진입하고 자동 재생한다.
- 미지원 파일 또는 복수 파일 드롭은 재생하지 않고 거부 상태만 표시한다.
- 드롭 안내 문구가 재생 화면에 남지 않도록 `PictureBox` 직접 그리기 방식으로 보정했다.

### 재생 최적화

- 1920x1080, 54~60fps 고비트레이트 영상 재생 성능 개선을 진행했다.
- 재생 중 강제 repaint 호출을 제거했다.
- 오버레이가 꺼진 일반 재생에서는 표시용 canvas clone을 생략하고 직접 Bitmap으로 변환한다.
- 오버레이가 꺼진 재생 경로에 10프레임 버퍼를 추가했다.
- 백그라운드에서 프레임을 읽고 Bitmap으로 준비한 뒤, UI 스레드는 준비된 이미지를 표시한다.
- OpenCV 백엔드가 지원하는 경우 하드웨어 가속 디코딩 속성을 시도한다.
- 하드웨어 가속 속성 적용 실패 또는 미지원 환경에서는 기존 소프트웨어 디코딩으로 동작한다.
- 설정창에 재생 최적화 모드를 추가했다.
  - `균형`: 오버레이가 꺼진 일반 재생에서 버퍼 경로 사용
  - `재생 우선`: 재생 속도를 우선하여 재생 오버레이를 생략하고 버퍼 경로 사용
  - `추적 우선`: 기존 분석/오버레이 의미를 우선하여 기존 재생 경로 사용

### 녹화/재생 전용 모드

- 실행 인수 `--reconly`, `/reconly`, `-reconly`로 녹화/재생 전용 모드를 켠다.
- 녹화/재생 전용 모드에서는 감지/오버레이 관련 일부 UI를 숨긴다.
- 저장된 녹화 모드가 `Auto`여도 녹화/재생 전용 모드에서는 `Manual` 또는 `Full` 중심으로 보정한다.

## 현재 작업 문서

- 작업 문서는 `tasks/*.md`에 번호형 파일명으로 정리한다.
- 현재 주요 task 문서:
  - `00_TEMPLATE.md`
  - `01_녹화재생전용.md`
  - `02_리팩토링_변수정책.md`
  - `03_현지화.md`
  - `04_동영상파일드래그드롭.md`
  - `05_재생최적화.md`
  - `06_Krypton_Toolkit_UI개선.md`
- 작업 완료 후 각 task 문서의 `결과` 섹션을 갱신한다.

## 주요 동작 기준

- 수동 녹화는 기존처럼 사용자가 시작/정지한다.
- 자동 녹화는 감지 상태 머신 기반으로 동작한다.
- 전체 녹화는 감지 상태와 독립적으로 연속 녹화하며, 설정된 간격마다 MP4 파일을 분리한다.
- 재생 오버레이 설정은 설정창의 Overlay 섹션에서 제어한다.
- 파일 선택 재생과 드래그드롭 재생은 같은 재생 흐름을 사용한다.
- 재생 최적화 모드는 기존 오버레이 의미를 유지하면서 성능 우선 경로를 선택할 수 있게 한다.

## 검증 상태

최근 확인한 빌드 명령:

```text
dotnet build DFBlackbox\DFBlackbox.csproj -o .buildcheck
```

결과:

```text
Warnings: 0
Errors: 0
```

- 마지막 문서 정리 전 Git 상태는 변경 사항 없이 깨끗했고, `main`과 `origin/main`이 일치했다.
- Krypton UI 변경은 빌드로 검증했으며, 실제 화면의 최종 시각 검증은 사용자 환경에서 이어서 확인해야 한다.

## 다음 확인 권장 사항

1. 설정창에서 비활성 `새로고침` 버튼의 배경과 글자 대비가 명확한지 확인한다.
2. 설정창 입력 컨트롤의 외곽선이 이중으로 겹쳐 보이지 않는지 확인한다.
3. 설정창 오른쪽의 해상도, FPS, RTSP/HTTP 및 저장소 항목이 잘리지 않는지 확인한다.
4. 메인 버튼과 설정 버튼의 기본, 마우스오버, 눌림, 비활성 상태가 일관적인지 확인한다.
5. 한국어와 영어에서 버튼 및 주요 라벨이 잘리지 않는지 확인한다.
6. 파일 선택과 드래그드롭 재생에서 재생 패널이 동일하게 표시되고 정상 동작하는지 회귀 확인한다.
7. 필요 시 실제 화면 확인 결과만 반영한 뒤 6번 작업을 최종 완료 처리한다.
